worker/node_modules/
worker/.dev.vars
worker/.wrangler/
.scratch/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Claude Code local settings (contiene comandos pre-aprobados con tokens inline)
.claude/settings.local.json

# Wrangler auto-config generado si se hace `wrangler deploy` desde el cwd
# equivocado. Si aparece en el root, es artefacto y debe borrarse.
wrangler.jsonc
legado-holding/

# Documentación comercial interna (pricing, márgenes, costos, propuestas)
docs/cost-and-pricing.md
docs/proposal-client.md
